package web_tracking

import "fmt"

templ OnTrackIdentifiactor(bookid uint) {
	<div>
		<button
			hx-get={ fmt.Sprintf("/tracking/%d", bookid) }
			hx-target="#progress-statistics"
			hx-swap="innerHTML"
			class="btn"
		>👀 tracking</button>
	</div>
}
